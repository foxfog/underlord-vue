# Примеры использования системы управления UI

## Примеры в JSON

### Скрыть все UI элементы
```json
{
  "type": "ui",
  "action": "hide"
}
```

### Показать все UI элементы
```json
{
  "type": "ui",
  "action": "show"
}
```

### Скрыть только hotbar (нижнюю панель)
```json
{
  "type": "ui",
  "target": ["hotbar"],
  "action": "hide"
}
```

### Скрыть только stats-button (кнопку статистики)
```json
{
  "type": "ui",
  "target": ["stats-button"],
  "action": "hide"
}
```

### Скрыть несколько элементов
```json
{
  "type": "ui",
  "target": ["hotbar", "stats-button"],
  "action": "hide"
}
```

### Скрыть всё, кроме диалога (для cinematics)
```json
{
  "type": "ui",
  "target": ["all", "dialogue"],
  "action": "hide"
}
```

## Сценарий: Intro сцена со скрытым UI

```json
{
  "id": "intro",
  "steps": [
    {
      "type": "ui",
      "action": "hide"
    },
    {
      "type": "titles",
      "text": "<h1>ДАВНЫМ-ДАВНО В ДАЛЕКОМ КОРОЛЕВСТВЕ...</h1>",
      "duration": 10000,
      "auto-end": true
    },
    {
      "type": "scene",
      "id": "throne_room"
    },
    {
      "type": "ui",
      "action": "show"
    },
    {
      "type": "dialogue",
      "character": "king",
      "text": "Добро пожаловать в мою обитель..."
    }
  ]
}
```

## Сценарий: Cinematic с только диалогом

```json
{
  "type": "ui",
  "target": ["all", "dialogue"],
  "action": "hide"
}
```

Это скроет все UI (hotbar и stats-button), но диалог останется видимым.

## Доступные идентификаторы

- `"all"` - все UI элементы
- `"stats-button"` - кнопка статистики 📊
- `"hotbar"` - нижняя панель (История, Меню, Сохранить, Загрузить)
- `"dialogue"` - блок диалога (не скрывается отдельно через UI, это часть story логики)

## Тестирование

Используйте файл `ui_test.json` для тестирования всех функций управления UI.
