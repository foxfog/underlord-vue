# UI Control System - Система управления видимостью UI элементов

## Описание

Система управления UI позволяет контролировать видимость UI элементов (кнопок, панелей) из JSON файлов сценария истории. Это полезно для создания cinematics, интро сцен и других мест, где нужно скрыть UI.

## Структура шага

```json
{
  "type": "ui",
  "action": "show" | "hide",
  "target": ["element-id-1", "element-id-2", "..."]
}
```

### Параметры

- **type** (обязательно): `"ui"` - тип шага для управления UI
- **action** (обязательно): `"show"` или `"hide"` - действие для применения
- **target** (опционально): массив идентификаторов элементов для управления
  - Если не указан или пуст - применяется ко ВСЕМ UI элементам
  - Специальное значение `"all"` - скрывает/показывает ВСЕ элементы
  - Конкретные идентификаторы - управляют отдельными элементами

## Доступные идентификаторы UI элементов

- `"stats-button"` - кнопка статистики персонажа (📊 Статы)
- `"hotbar"` - нижняя панель с кнопками (История, Главное меню, Сохранить, Загрузить)
- `"dialogue"` - блок диалога (текст и выбор вариантов)
- `"all"` - все UI элементы

## Примеры использования

### Пример 1: Скрыть все UI элементы

```json
{
  "type": "ui",
  "action": "hide"
}
```

Это скроет все UI, так как `target` не указан.

### Пример 2: Скрыть только hotbar и dialogue

```json
{
  "type": "ui",
  "action": "hide",
  "target": ["hotbar", "dialogue"]
}
```

При этом кнопка статистики останется видимой.

### Пример 3: Показать все UI после скрытия

```json
{
  "type": "ui",
  "action": "show",
  "target": ["all"]
}
```

или просто:

```json
{
  "type": "ui",
  "action": "show"
}
```

### Пример 4: Скрыть все, кроме диалога

```json
{
  "type": "ui",
  "action": "hide",
  "target": ["all", "dialogue"]
}
```

Это скроет все UI элементы, а затем покажет только диалог.

## Сценарий использования: Intro/Credits сцена

```json
{
  "id": "intro",
  "steps": [
    {
      "type": "ui",
      "action": "hide",
      "target": ["all"]
    },
    {
      "type": "titles",
      "text": "<h1>Star Wars Opening Crawl</h1>",
      "duration": 20000,
      "auto-end": true
    },
    {
      "type": "scene",
      "id": "game_start"
    },
    {
      "type": "ui",
      "action": "show",
      "target": ["all"]
    },
    {
      "type": "dialogue",
      "character": "mc",
      "text": "Добро пожаловать в игру!"
    }
  ]
}
```

## Техническая реализация

### В composable `useVisualNovel.js`:

- Добавлен `uiVisibility` ref с состоянием видимости каждого элемента
- Функция `handleUIStep(step)` обрабатывает шаги типа `"ui"`
- Состояние передается в компонент `Game.vue` через `defineExpose`

### В компоненте `Game.vue`:

- Состояние `uiVisibility` отслеживается через watch
- Вычисляемые свойства `showStatsButton` и `showHotbar` контролируют видимость элементов
- Директивы `v-if` скрывают/показывают элементы на основе вычисляемых свойств

### В компоненте `VisualNovel.vue`:

- `uiVisibility` экспортируется через `defineExpose` для доступа из родительского компонента

## Примечания

1. **Скрытие в JSON vs CSS**: UI элементы полностью удаляются из DOM при скрытии (используется `v-if`), а не только скрываются визуально. Это предотвращает взаимодействие с невидимыми элементами.

2. **Логика AND с "all"**: При использовании `target: ["all", "dialogue"]`, система сначала скроет ВСЕ элементы, а затем покажет только диалог. Это создает логику "all EXCEPT dialogue".

3. **Состояние по умолчанию**: При загрузке игры все UI элементы видны по умолчанию.

4. **Сохранение состояния**: Текущее состояние видимости UI НЕ сохраняется в save файлы (так как это обычно состояние игровой сцены, а не игрового прогресса).

## Отладка

В консоли браузера вы увидите логи вроде:
```
UI hide: all elements
UI show: stats-button
UI hide: hotbar, dialogue
```

Это помогает отследить, какие шаги UI обрабатываются.
