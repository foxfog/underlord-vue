# 🚀 QUICK START: Работа с перенесёнными стилями

Этот файл поможет быстро начать работать с новой структурой стилей.

## За 60 секунд

### 1. Где находятся стили?
```
src/renderer/public/styles/game/
├── visual-novel.css        👈 Диалоги, фоны
├── character.css           👈 Персонажи
├── character-stats-modal.css 👈 Статистика персонажа
├── text-input-modal.css    👈 Ввод текста
└── equip-part.css          👈 Экипировка
```

### 2. Как найти стили для компонента?

| Компонент | Файл |
|-----------|------|
| `DialogueBox.vue` | `visual-novel.css` |
| `Character.vue` | `character.css` |
| `CharacterStatsModal.vue` | `character-stats-modal.css` |
| `TextInputModal.vue` | `text-input-modal.css` |
| `EquipPart.vue` | `equip-part.css` |

### 3. Как отредактировать стиль?

**Пример:** Изменить цвет спикера в диалоге

1. Откройте `src/renderer/public/styles/game/visual-novel.css`
2. Найдите селектор `.speaker`
3. Измените значение `color`
4. Сохраните файл ✅ (горячая перезагрузка сработает автоматически)

```css
.dialogue-box {
	.speaker {
		color: #ffcc00;  👈 Отредактируйте здесь
	}
}
```

### 4. Как добавить новый стиль?

```css
.dialogue-box {
	/* существующие стили */
	
	.new-element {  👈 Добавьте новый селектор
		color: blue;
		margin-top: 10px;
	}
}
```

## Примеры быстрых задач

### Изменить фон диалога

**Файл:** `visual-novel.css`

```css
.dialogue-box {
	background: rgba(0, 0, 0, 0.8);  👈 Измените сюда
}
```

### Изменить размер заголовка

**Файл:** `visual-novel.css`

```css
.title-content {
	font-size: 1.8em;  👈 Измените на нужный размер
}
```

### Изменить цвет кнопки выбора

**Файл:** `visual-novel.css`

```css
.choice-btn {
	background: #444;  👈 Измените сюда
	
	&:hover {
		background: #555;  👈 И цвет при hover
	}
}
```

### Изменить стили полосы здоровья

**Файл:** `character-stats-modal.css`

```css
.stat-fill {
	background-color: #4caf50;  👈 Здоровье (зелёный)
	
	&.mp {
		background-color: #2196f3;  👈 Мана (синий)
	}
}
```

## CSS Nesting справка

**Что это?**
```css
/* Обычный CSS */
.parent { }
.parent .child { }

/* CSS Nesting (новое) */
.parent {
	.child {
		/* вложенный селектор */
	}
}
```

**Используется везде в проекте:**
```css
.dialogue-box {           👈 Родитель
	.speaker { }          👈 Дочерний класс
	.choices {            👈 Дочерний контейнер
		.choice-btn { }   👈 Глубокая вложенность
	}
}
```

**С псевдоклассами:**
```css
.btn {
	background: blue;
	
	&:hover {      👈 & = .btn
		background: darkblue;
	}
	
	&:disabled {   👈 & = .btn
		opacity: 0.5;
	}
}
```

## Полезные команды

### Проверить синтаксис
```bash
npm run build
```

### Запустить разработку
```bash
npm run dev
```

### Просмотреть структуру CSS
```bash
cat src/renderer/public/styles/game/visual-novel.css
```

## Частые ошибки

### ❌ Неправильно
```css
/* Стили в Vue файле */
<style>
.dialogue-box { color: red; }
</style>
```

### ✅ Правильно
```css
/* В файле visual-novel.css */
.dialogue-box {
	color: red;
}
```

---

## Нужна помощь?

📖 **Подробное руководство:** `GAME_STYLES_USAGE_GUIDE.md`  
📊 **Статистика:** `STYLES_MIGRATION_METRICS.md`  
📝 **История:** `GAME_STYLES_MIGRATION.md`  
🎓 **Документация CSS:** `src/renderer/public/styles/game/README.md`

## Контрольный список для новых разработчиков

- ☑ Понимаю, где находятся стили игровых компонентов
- ☑ Знаю, как найти стили для нужного компонента
- ☑ Могу отредактировать существующий стиль
- ☑ Понимаю синтаксис CSS Nesting
- ☑ Знаю, что горячая перезагрузка работает автоматически
- ☑ Прочитал примеры выше

**Если все пункты отмечены - вы готовы! 🎉**

---

**Последнее обновление:** 2026-02-05  
**Статус:** ✅ Активно используется
