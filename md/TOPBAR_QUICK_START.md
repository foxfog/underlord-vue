# Топбар и Инвентарь - Быстрый старт

## 🚀 Что было сделано

Добавлена **верхняя панель (Topbar)** с кнопками для открытия:
1. **Статистики персонажа** 📊
2. **Инвентаря с 3 вкладками** 🎒
   - Инвентарь (список предметов)
   - Статистика (HP, MP, Атака, Защита)
   - Способности (список умений)

## 📁 Созданные файлы

```
src/renderer/components/game/
├── Topbar.vue              # Верхняя панель с кнопками
└── InventoryModal.vue      # Модальное окно инвентаря с табами

md/
├── TOPBAR_INVENTORY_MODAL.md       # Подробная документация
├── TOPBAR_INVENTORY_CHANGELOG.md   # Лог изменений
├── TOPBAR_INVENTORY_ARCHITECTURE.md # Архитектура
└── TOPBAR_INVENTORY_FINAL.md       # Финальное резюме
```

## 🔧 Основные компоненты

### Topbar.vue (50 строк)
```vue
<Topbar
  :character="mcCharacter"
  @open-stats="toggleStatsModal"
  @open-inventory="toggleInventoryModal"
/>
```

**Кнопки:**
- 📊 Статы → открывает CharacterStatsModal
- 🎒 Инвентарь → открывает InventoryModal

### InventoryModal.vue (260 строк)
```vue
<InventoryModal
  :is-visible="showInventoryModal"
  :character="mcCharacter"
  :items-data="itemsData"
  @close="toggleInventoryModal"
/>
```

**Вкладки:**
- 🎒 Инвентарь - список предметов
- 📊 Статистика - HP, MP, Атака, Защита
- ✨ Способности - список способностей

## 📊 Структура данных

### Персонаж (character)
```json
{
  "id": "mc",
  "name": "Я",
  "hp": 10,
  "hpmax": 10,
  "mp": 10,
  "mpmax": 10,
  "attack": 2,
  "defense": 1,
  
  "inventory": {
    "items": [
      { "itemId": "gasmask" },
      { "itemId": "ygdrasil-coin-old", "quantity": 23 }
    ]
  },
  
  "abilities": [
    {
      "id": "power_attack",
      "name": "Мощная атака",
      "description": "Нанести увеличенный урон врагу..."
    }
  ]
}
```

### Предметы (itemsData)
Загружаются автоматически из:
- `/data/characters/equipment.json` - экипировка
- `/data/characters/other.json` - расходники, валюта

```json
{
  "id": "gasmask",
  "part": "head",
  "weight": 0.5,
  "name": "Противогаз"
}
```

## 🎨 Дизайн

### Топбар
- Позиция: фиксированная вверху
- Фон: черный с градиентом (блюр эффект)
- Кнопки: полупрозрачные, с hover эффектом
- Z-index: 100

### Модальное окно
- Размер: 400-650px ширина
- Фон: темный серый #2c2c2c
- Вкладки: голубые когда активны (#4a9eff)
- Z-index: 1000

## 🔌 Интеграция

### В Game.vue
```javascript
// Импорты
import Topbar from '../components/game/Topbar.vue'
import InventoryModal from '../components/game/InventoryModal.vue'

// Refs
const showInventoryModal = ref(false)
const itemsData = ref({})

// Функции
function toggleInventoryModal() {
  showInventoryModal.value = !showInventoryModal.value
}

// При загрузке
onMounted(() => {
  Promise.all([
    fetch('/data/characters/equipment.json').then(r => r.json()),
    fetch('/data/characters/other.json').then(r => r.json())
  ]).then(([equipment, other]) => {
    itemsData.value = {}
    equipment.forEach(item => itemsData.value[item.id] = item)
    other.forEach(item => itemsData.value[item.id] = item)
  })
})
```

## ✅ Проверка

- ✅ Компоненты созданы и работают
- ✅ Нет ошибок компиляции
- ✅ HMR обновляет компоненты
- ✅ Данные загружаются из JSON
- ✅ Вкладки переключаются
- ✅ События работают корректно

## 🎯 Использование

1. **В игре нажимаем кнопку [🎒 Инвентарь] на Topbar**
2. **Открывается модальное окно с вкладками**
3. **Переходим между вкладками кликом**
4. **Закрываем кнопкой × или кликом вне окна**

## 🔄 Управление видимостью

```javascript
// Скрыть/показать Topbar
uiVisibility.value.topbar = false  // скрыть
uiVisibility.value.topbar = true   // показать

// Открыть/закрыть инвентарь
showInventoryModal.value = true   // открыть
showInventoryModal.value = false  // закрыть
```

## 📈 Расширение

### Добавить новую вкладку
```javascript
// В InventoryModal.vue
const tabs = [
  { id: 'inventory', label: '🎒 Инвентарь' },
  { id: 'statistics', label: '📊 Статистика' },
  { id: 'abilities', label: '✨ Способности' },
  { id: 'equipment', label: '👔 Экипировка' }  // Новая вкладка
]

// Добавить в template
<div v-show="activeTab === 'equipment'" class="tab-content">
  <!-- Содержимое вкладки -->
</div>
```

### Добавить способность персонажу
```json
{
  "abilities": [
    {
      "id": "new_ability",
      "name": "Название способности",
      "description": "Описание того, что делает способность"
    }
  ]
}
```

## 📚 Файлы документации

- `TOPBAR_INVENTORY_MODAL.md` - подробное описание компонентов
- `TOPBAR_INVENTORY_CHANGELOG.md` - полный лог всех изменений
- `TOPBAR_INVENTORY_ARCHITECTURE.md` - диаграммы и архитектура
- `TOPBAR_INVENTORY_FINAL.md` - финальное резюме

## 💡 Советы

1. **Для добавления новых предметов** - обновите equipment.json или other.json
2. **Для добавления способностей** - добавьте в abilities массив персонажа
3. **Для изменения стилей** - отредактируйте Topbar.vue и InventoryModal.vue (style scoped)
4. **Для скрытия Topbar** - установите `uiVisibility.topbar = false`

## 🚨 Если что-то не работает

1. Проверьте консоль браузера на ошибки
2. Убедитесь, что JSON файлы парсятся правильно
3. Проверьте, что character имеет inventory и abilities
4. Перезагрузите приложение (npm run dev)

## 🎉 Готово!

Система полностью работает и готова к использованию. Наслаждайтесь! 🚀
