// Centralized sound constants and aliases

// Single source of truth for sound file paths
export const SOUND_CLOTH = 'audio/sound/cloth.ogg'

// Alias map for convenient usage in scripts (e.g. story steps)
export const SOUND_ALIASES = {
	cloth: SOUND_CLOTH
}

